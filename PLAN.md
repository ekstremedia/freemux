1. Enhanche the user interface to make it more intuitive and user-friendly.
2. Source tab:
    - Only source input and its probe is interesting here, remove output.
    - Make possibility to add multiple sources / add folder, and when added, display them in a list. Should be able to remove or add files to list. If this is the case, we need to have the convert\output screen match.
    - Added videos should be able to be previewed in a video player window, which is intuitive and easy to use and low on resource usage. With playback controls etc.
3. Convert tab:
   - Rename this to Convertion or something more intuitive.
   - Remove conversion progress, lets make this into the last tab. Same with command preview.
   - For profiles, Save as new is not working properly
   - remove output file part from the convert tab.
   - Clean up the profile management, make it more intuitive and user-friendly. Maybe add a profile manager tab where you can manage all your profiles in one place.
3. New tab: Output 
4. Output tab:
   - This is where the user can see the output of the conversion, including the progress and the command preview. Make the command preview copy-able with a copy button. Filenames should be quouted if they contain spaces, and the command should be formatted in a way that is easy to read and understand.
   - Make it more intuitive and user-friendly, with clear indicators of progress and easy access to the command preview.
   - The output tab should show, in a elegant and neatly way: The input file, and short summary of resolution, bitrate, and duration, size, codecs, etc. And short summart of conversion settings. And here is where we set a output file name and location, if 1 file, or folder, if multiple files. Figure out a good way to handle both 1 and several files here.
   - Then a big START CONVERSION button, and when clicked, it starts the conversion and shows the progress. Maybe also a cancel button to stop the conversion if needed. Add a progress per file - and a progress overall.
   - Add a button to open the output folder when conversion is done. And maybe also a button to open the converted file directly.

If i forget something, please suggest. Make the interface as intuitive and user-friendly as possible, with clear indicators and easy access to all features. Make it responsive and visually pleasing.