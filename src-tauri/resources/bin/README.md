# Bundled FFmpeg binaries

Place release binaries here before packaging the desktop app.

Expected layout:

```text
windows/ffmpeg.exe
windows/ffprobe.exe
macos/ffmpeg
macos/ffprobe
linux/ffmpeg
linux/ffprobe
```
